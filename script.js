const BOARD_ZISE = 15;
let dataModel;
initialize(BOARD_ZISE);

const initialize = (size) => {
    dataModel = createDataModel(size);
    createDOM(size);
};

const createDataModel = (size) => {
    
};

const createDOM = (size) => {
    
};